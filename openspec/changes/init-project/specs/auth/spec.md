# Authentication Spec

## Requirements
- Users must sign up/login to access the dashboard.
- Use Supabase Auth (Email/Password).
- Redirect to `/login` if not authenticated.
- Redirect to `/` after successful login.
- Show "Logout" button in the dashboard.
